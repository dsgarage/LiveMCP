{
  "name": "livemcp",
  "version": "0.1.0",
  "private": true,
  "description": "Ableton Live 内で完結する MCP サーバー（M4L デバイス内 node.script で動作）",
  "scripts": {
    "build": "npm --prefix server run build && node scripts/build-device.js",
    "install-device": "npm run build && node scripts/build-device.js --install",
    "test": "npm --prefix server test"
  }
}
